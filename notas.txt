PSeudoCodigo.


listElements = (a,e,i,o,u); // Elementos del juego

aleatoryCola (Random(listElements)); // creamos una cola de elements random.

UserCola = (input());  // Creamos cola donde user almacena 

contador = 0 // Para score de usuario


ConsoleWriteline (First element of the list); 

listUser = input(Console.ReadLine(Introduce el numero ))

Do listUser == aleatoryList();
	
	agregar new element a queue aleatoryList.
	mostrar new element random.
	listUser = Pedir input user del nuevo elemento

		if listUser == aleatoryList {
	
			contador +=;
	
			queue aleatoryList (Random (listElement));
	
			}
		else {
			
			Console.WriteLine("El juego terminó, Tu puntaje es {0}", contador);
			break.
			}
while

		